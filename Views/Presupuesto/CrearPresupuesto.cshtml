@{
    ViewData["Title"] = "CrearPresupuesto";
}

@model espacioPresupuestoViewModel.PresupuestoListaCliente

<table class="table table-primary table-striped">
    <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Nombre</th>
      <th scope="col">Email</th>
      <th scope="col">Telefono</th>
    </tr>
  </thead>
  <tbody> 
    @foreach(var cliente in Model.ListaClientes)
    {
        <tr>
            <th scope="row">@cliente.ClienteId</th>
            <td>@cliente.Nombre</td>
            <td>@cliente.Email</td>
            <td>$@cliente.Telefono</td>
        </tr>
    }
  </tbody>
</table>

<form asp-controller="Presupuesto" asp-action="CrearPresupuesto">
  <div class="mb-3">
    <label asp-for=@Model.Presupuesto.Cliente.ClienteId class="form-label">Cliente ID</label>
    <input asp-for=@Model.Presupuesto.Cliente.ClienteId type="number" class="form-control">
    <label asp-for=@Model.Presupuesto.FechaCreacion class="form-label">Fecha creación</label>
    <input asp-for=@Model.Presupuesto.FechaCreacion type="date" class="form-control">
  </div>    
  <button type="submit" class="btn btn-outline-success">Crear</button>
</form>